---
id: TASK-0008
title: Build eidos do <task-id> — the orchestrating engine
status: In Progress
created: '2026-05-14'
updated: '2026-05-14'
---
The canonical work-execution verb. Runs the full discipline: STUDY (load task + telos + active guardrails + relevant praxis turns) → RESEARCH (search prior findings) → ADVERSARIAL pass (gotchas, unknown unknowns, false premises) → PLAN → CARDINALITY check (Solo by default; escalate if triggers fire) → ACT → VERIFY (forge contracts + anti-goals not violated) → LEARN (praxis turn) → DOCUMENT (system-of-record routing per task tags) → META (log plugin candidate; do not auto-build). This is the integration point of every primitive — telos, four forges, Pod cardinality, system-of-record routing, praxis learning loop. ~3-5 days. Depends on TASK-0001 (path resolution) + TASK-0006 (praxis-md rename) + TASK-0005 (eidos tick) landing first.
